// Create variables with empty default values
ext["RELEASE_STORE_FILE"] = "/"
ext["RELEASE_STORE_PASSWORD"] = ""
ext["RELEASE_KEY_ALIAS"] = ""
ext["RELEASE_KEY_PASSWORD"] = ""

File secretPropsFile = project.rootProject.file('local.properties')
if (secretPropsFile.exists()) {
    Properties p = new Properties()
    new FileInputStream(secretPropsFile).withCloseable { is -> p.load(is) }
    p.each { name, value -> ext[name] = value }
} else {
    ext["RELEASE_STORE_FILE"] = System.getenv('RELEASE_STORE_FILE')
    ext["RELEASE_STORE_PASSWORD"] = System.getenv('RELEASE_STORE_PASSWORD')
    ext["RELEASE_KEY_ALIAS"] = System.getenv('RELEASE_KEY_ALIAS')
    ext["RELEASE_KEY_PASSWORD"] = System.getenv('RELEASE_KEY_PASSWORD')
}
